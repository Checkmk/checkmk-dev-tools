## Do not raise Fatal errors if no-raise flag is specified
<!--
type: breaking
scope: all
affected: all
-->

This fixes throwing `Fatal exception: The build we started has result=FAILURE <URL>` even if the flag `--no-raise` is set.
Furthermore the script terminates with exit code zero if the flag `--no-raise` is set.
