## Fix compressed artifact download for files only
<!--
type: bugfix
scope: all
affected: all
-->

With version `2.4.0` artifacts are downloaded as compressed archive instead of file by file. This approach failed as soon as the compressed archive only contained files instead of nested folders.
With this fix the compressed archive can now consist of nested folders or individual files. Further more is the downloaded archive ZIP file deleted after decompression.
