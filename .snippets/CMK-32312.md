## Fix call to missing ignore_build_queue attribute
<!--
type: bugfix
scope: all
affected: all
-->

Executing `_fn_fetch` directly without supplying `args` could lead to
an error like this:

```
AttributeError: 'Namespace' object has no attribute 'ignore_build_queue'
```

This is now fixed.

