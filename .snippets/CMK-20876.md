## Set download true for fetch function
<!--
type: bugfix
scope: all
affected: all
-->

The function `_fn_fetch` is missing the arg parse argument `download` to
actually download the files from remote
